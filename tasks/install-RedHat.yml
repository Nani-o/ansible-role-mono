---
- name: Add mono yum repo
  yum_repository:
    name: mono
    baseurl: "{{ mono_yum_repo }}"
    gpgkey: "{{ mono_yum_gpgkey }}"
    description: "{{ mono_yum_description }}"
    state: present

- name: Install mono
  yum:
    name: "{{ mono_package }}"
    state: latest
